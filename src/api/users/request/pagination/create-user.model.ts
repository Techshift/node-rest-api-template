/**
 * @swagger
 * components:
 *   schemas:
 *     CreateUserModel:
 *       required:
 *         - name
 *       properties:
 *         name:
 *           type: string
 */
export interface CreateUserModel {
	name: string;
}
